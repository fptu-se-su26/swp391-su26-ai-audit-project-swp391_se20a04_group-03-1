Mobile dependency audit

Scanned files: `src/mobile/package.json` and `src/mobile/src` imports.

Findings:

- Web-only or web-focused libraries found:
  - `@radix-ui/react-dialog` / `radix-ui` — Radix is web-only (React DOM). Replace with RN modal libraries such as `react-native-modal` or use built-in `Modal`.
  - `@zxing/browser` / `zxing` — browser QR scanning. For React Native use `react-native-vision-camera` or `react-native-camera` with `react-native-qrcode-scanner`.
  - `lucide-react` — React SVG icon set for web. Use `react-native-vector-icons` or `react-native-svg` + `react-native-svg-transformer`.
  - `framer-motion` — web animation library. For RN use `react-native-reanimated` + `react-native-gesture-handler` or `moti` (based on reanimated).
  - `radix-ui` imports (Label, Slot) used in UI components — need RN equivalents or custom components.

- DOM usages detected in source (e.g., `window`, `window.open`, `EventTarget`): these require platform-specific handling. Keep web code in web folder or guard with `Platform.OS === 'web'`.

Recommendations:

1. Decide target approach:
   - Option A (Native-first): Migrate mobile folder to pure React Native components and replace web libs with RN equivalents.
   - Option B (Hybrid): Keep current web-oriented components and run as web app (Next.js) while building a separate RN app with adapted components.

2. Suggested replacements (quick mapping):
   - `@radix-ui/react-dialog` -> `react-native-modal` or `Modal` from `react-native`.
   - `@zxing/browser` -> `react-native-vision-camera` + `vision-camera-code-scanner` OR `react-native-camera` + `react-native-qrcode-scanner`.
   - `lucide-react` -> `react-native-vector-icons` (or use `react-native-svg` + convert icons to SVG components).
   - `framer-motion` -> `moti` / `react-native-reanimated`.

3. TypeScript / tsconfig:
   - Keep `DOM` types only if you need web code in `src/mobile`. Prefer to split tsconfigs (`tsconfig.mobile.json` without DOM) if targeting pure RN.

4. Incremental plan:
   - Replace one feature at a time (QR scanner -> Map screen -> Dialogs).
   - Create RN POC for QR scanning to validate native permissions and camera performance.

Notes for developers:

- Installing `react-native-vision-camera` requires native setup (iOS/Android pods and permission config). If you prefer zero-native-change dev, use Expo + `expo-camera`.
- Keep `types` shims for web-only libs until migration completes.

Generated by automated scan on May 26, 2026.
